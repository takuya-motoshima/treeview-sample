/**
 * Types The type name assigned to the folder by the plugin.
 * 
 * @type {string}
 */
const folder = 'folder';

/**
 * Tpes The type name to assign to the selected folder in the plugin.
 * 
 * @type {string}
 */
const selectedFolder = 'selectedFolder';

export {folder, selectedFolder}
